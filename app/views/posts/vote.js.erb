<% if @vote.errors.any? %>
  $("#messages").html('').slideUp();
  $("#messages").html("<%= @vote.errors.full_messages.join(', ') %>").slideDown();
<% else %>
  $("#post_<%= @post.id %>_votes").html("<%= @post.total_votes %>")
<% end %>
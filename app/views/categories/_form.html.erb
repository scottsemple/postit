<%= render "shared/errors", object: @category %>

<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />
  <%= f.submit 'Create category', class: 'btn' %>
<% end %>